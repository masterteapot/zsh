#!/bin/sh
alias vi='nvim'
alias g='lazygit'
alias zsh-update-plugins="find "$ZDOTDIR/plugins" -type d -exec test -e '{}/.git' ';' -print0 | xargs -I {} -0 git -C {} pull -q"
alias nvimrc='nvim ~/.config/nvim/'
alias notes='nvim ~/Nextcloud/Notes/'
alias zshrc='nvim ~/.config/zsh/'
<<<<<<< HEAD
alias blog='nvim ~/Website_Projects/jaredsblog/jaredsblog/'
alias django='source ~/Website_Projects/jaredsblog/jareds-venv/bin/activate'
=======
alias blog='cd ~/Website_Projects/jaredsblog/jaredsblog/'
alias django='source ~/.pyenv/django/bin/activate'
>>>>>>> d8e6e888049fce7ad8b3b3b23e711dd4a3b3ab5e


# Colorize grep output (good for log files)
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# confirm before overwriting something
alias cp="cp -i"
alias mv='mv -i'

# easier to read disk
alias df='df -h'     # human-readable sizes
alias free='free -m' # show sizes in MB

# get top process eating memory
alias psmem='ps auxf | sort -nr -k 4 | head -5'

# get top process eating cpu ##
alias pscpu='ps auxf | sort -nr -k 3 | head -5'

# Better ls with colors
if [ -x "$(command -v exa)" ]; then
    alias ls="exa"
    alias la="exa --long --all --group --header --accessed --icons --classify"
    alias ll="exa --long --group --header --accessed --icons --classify"
fi

# Delete things to the trash
if [ -x "$(command -v trash)" ]; then
    alias dd="trash"
    alias delete="trash"
    alias rm="trash -i"
fi

# Aliases for random programs
if [ -x "$(command -v thunderbird)" ]; then
  alias thunderbird='nohup thunderbird > ~/.nohup_out &'
fi


# FZF aliases
alias fzff='fdfind --type f --strip-cwd-prefix --hidden --exclude .git | fzf'
alias fzfd='fdfind --type d --strip-cwd-prefix --hidden --exclude .git | fzf'

# FD aliases
alias fd="fdfind"


# Do a search through history
<<<<<<< HEAD
alias hg='history | fzf'

=======
alias hg='sort ~/.config/zsh/.zsh_history | uniq -c | sort | fzf'
>>>>>>> d8e6e888049fce7ad8b3b3b23e711dd4a3b3ab5e

# Clear the terminal
alias cl="clear"
alias cln="clear && echo '' && neofetch"

# Quick export of testamentto
alias wp="code /var/www/contracts/"
alias contracts="nvim /var/www/contracts/"
alias zshrc="nvim ~/.config/zsh/"
alias wp-contracts-prod="~/.jaredbs/wp-contracts-prod.sh"
alias wp-alshaya-prod="~/.jaredbs/wp-alshaya-prod.sh"
